/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MeetingProjection } from '../../models/meeting-projection';
import { User } from '../../models/user';

export interface InviteToMeeting$Params {
  meeting_id: number;
      body: Array<User>
}

export function inviteToMeeting(http: HttpClient, rootUrl: string, params: InviteToMeeting$Params, context?: HttpContext): Observable<StrictHttpResponse<MeetingProjection>> {
  const rb = new RequestBuilder(rootUrl, inviteToMeeting.PATH, 'patch');
  if (params) {
    rb.path('meeting_id', params.meeting_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MeetingProjection>;
    })
  );
}

inviteToMeeting.PATH = '/meeting/invite-to-meeting/{meeting_id}';
