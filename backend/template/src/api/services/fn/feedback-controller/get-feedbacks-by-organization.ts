/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Feedback } from '../../models/feedback';

export interface GetFeedbacksByOrganization$Params {
  organisationId: number;
}

export function getFeedbacksByOrganization(http: HttpClient, rootUrl: string, params: GetFeedbacksByOrganization$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Feedback>>> {
  const rb = new RequestBuilder(rootUrl, getFeedbacksByOrganization.PATH, 'get');
  if (params) {
    rb.path('organisationId', params.organisationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Feedback>>;
    })
  );
}

getFeedbacksByOrganization.PATH = '/feedbacks/organization/{organisationId}';
