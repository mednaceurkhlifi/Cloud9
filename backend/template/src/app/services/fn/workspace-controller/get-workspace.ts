/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WorkspaceResponse } from '../../models/workspace-response';

export interface GetWorkspace$Params {
  organization_id: number;
  size: number;
  page_no: number;
}

export function getWorkspace(http: HttpClient, rootUrl: string, params: GetWorkspace$Params, context?: HttpContext): Observable<StrictHttpResponse<WorkspaceResponse>> {
  const rb = new RequestBuilder(rootUrl, getWorkspace.PATH, 'get');
  if (params) {
    rb.path('organization_id', params.organization_id, {});
    rb.path('size', params.size, {});
    rb.path('page_no', params.page_no, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WorkspaceResponse>;
    })
  );
}

getWorkspace.PATH = '/workspace/get-workspace/{organization_id}/{size}/{page_no}';
