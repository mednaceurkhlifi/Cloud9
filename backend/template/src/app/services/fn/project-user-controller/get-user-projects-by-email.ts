/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProjectUserProjection } from '../../models/project-user-projection';

export interface GetUserProjectsByEmail$Params {
  user_email: string;
}

export function getUserProjectsByEmail(http: HttpClient, rootUrl: string, params: GetUserProjectsByEmail$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProjectUserProjection>>> {
  const rb = new RequestBuilder(rootUrl, getUserProjectsByEmail.PATH, 'get');
  if (params) {
    rb.path('user_email', params.user_email, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProjectUserProjection>>;
    })
  );
}

getUserProjectsByEmail.PATH = '/project-user/get-projects/{user_email}';
