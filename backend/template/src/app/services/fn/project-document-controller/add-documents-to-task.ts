/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DocumentRequest } from '../../models/document-request';
import { ProjectDocument } from '../../models/project-document';

export interface AddDocumentsToTask$Params {
  task_id: number;
      body?: {
'documents_request': Array<DocumentRequest>;
'documents': Array<Blob>;
}
}

export function addDocumentsToTask(http: HttpClient, rootUrl: string, params: AddDocumentsToTask$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProjectDocument>>> {
  const rb = new RequestBuilder(rootUrl, addDocumentsToTask.PATH, 'patch');
  if (params) {
    rb.path('task_id', params.task_id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProjectDocument>>;
    })
  );
}

addDocumentsToTask.PATH = '/project-document/add-documents-module/{task_id}';
