/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DocumentRequest } from '../../models/document-request';
import { ProjectDocument } from '../../models/project-document';

export interface AddDocumentsToModule$Params {
  module_id: number;
      body?: {
'documents_request': Array<DocumentRequest>;
'documents': Array<Blob>;
}
}

export function addDocumentsToModule(http: HttpClient, rootUrl: string, params: AddDocumentsToModule$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProjectDocument>>> {
  const rb = new RequestBuilder(rootUrl, addDocumentsToModule.PATH, 'patch');
  if (params) {
    rb.path('module_id', params.module_id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProjectDocument>>;
    })
  );
}

addDocumentsToModule.PATH = '/project-document/add-documents-module/{module_id}';
