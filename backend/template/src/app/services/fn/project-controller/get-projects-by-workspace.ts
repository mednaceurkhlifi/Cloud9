/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProjectResponse } from '../../models/project-response';

export interface GetProjectsByWorkspace$Params {
  workspace_id: number;
  size: number;
  page_no: number;
}

export function getProjectsByWorkspace(http: HttpClient, rootUrl: string, params: GetProjectsByWorkspace$Params, context?: HttpContext): Observable<StrictHttpResponse<ProjectResponse>> {
  const rb = new RequestBuilder(rootUrl, getProjectsByWorkspace.PATH, 'get');
  if (params) {
    rb.path('workspace_id', params.workspace_id, {});
    rb.path('size', params.size, {});
    rb.path('page_no', params.page_no, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProjectResponse>;
    })
  );
}

getProjectsByWorkspace.PATH = '/project/get-projects/{workspace_id}/{size}/{page_no}';
