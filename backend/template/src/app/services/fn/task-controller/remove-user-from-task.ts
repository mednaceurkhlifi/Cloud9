/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface RemoveUserFromTask$Params {
  task_id: number;
  user_email: string;
}

export function removeUserFromTask(http: HttpClient, rootUrl: string, params: RemoveUserFromTask$Params, context?: HttpContext): Observable<StrictHttpResponse<{
[key: string]: string;
}>> {
  const rb = new RequestBuilder(rootUrl, removeUserFromTask.PATH, 'patch');
  if (params) {
    rb.path('task_id', params.task_id, {});
    rb.path('user_email', params.user_email, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      [key: string]: string;
      }>;
    })
  );
}

removeUserFromTask.PATH = '/task/remove-user/{task_id}/{user_email}';
