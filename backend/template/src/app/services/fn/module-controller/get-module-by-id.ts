/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProjectModuleProjection } from '../../models/project-module-projection';

export interface GetModuleById$Params {
  module_id: number;
}

export function getModuleById(http: HttpClient, rootUrl: string, params: GetModuleById$Params, context?: HttpContext): Observable<StrictHttpResponse<ProjectModuleProjection>> {
  const rb = new RequestBuilder(rootUrl, getModuleById.PATH, 'get');
  if (params) {
    rb.path('module_id', params.module_id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProjectModuleProjection>;
    })
  );
}

getModuleById.PATH = '/project-module/get-module/{module_id}';
