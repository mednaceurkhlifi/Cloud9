/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ModuleResponse } from '../../models/module-response';

export interface GetModulesByProject$Params {
  project_id: number;
  size: number;
  page_no: number;
}

export function getModulesByProject(http: HttpClient, rootUrl: string, params: GetModulesByProject$Params, context?: HttpContext): Observable<StrictHttpResponse<ModuleResponse>> {
  const rb = new RequestBuilder(rootUrl, getModulesByProject.PATH, 'get');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('size', params.size, {});
    rb.path('page_no', params.page_no, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ModuleResponse>;
    })
  );
}

getModulesByProject.PATH = '/project-module/get-modules/{project_id}/{size}/{page_no}';
